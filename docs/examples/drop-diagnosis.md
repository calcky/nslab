# Drop Diagnosis Has Moved

Drop diagnosis is now part of [Kernel data path observation](kernel-path.md), alongside normal forwarding and local INPUT/OUTPUT experiments. The example directory is `examples/kernel-path`.
