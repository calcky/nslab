# DNS service is disabled; this process serves only RA and DHCPv6.
port=0
interface=eth0
bind-interfaces
no-hosts
no-resolv
enable-ra
ra-param=eth0,10,30
dhcp-range=2001:db8:60::100,2001:db8:60::1ff,64,5m
log-dhcp
